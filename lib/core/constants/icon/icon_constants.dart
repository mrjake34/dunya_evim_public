import 'package:flutter/material.dart';

class IconConstants {
  IconConstants._init();
  static const Icon euSymbol = const Icon(Icons.euro);
  static const Icon usdSymbol = const Icon(Icons.attach_money);
  static const Icon tlSymbol = const Icon(Icons.currency_lira);
  static const Icon poundSymbol = const Icon(Icons.currency_pound);
  static const Icon defaultSymbol = const Icon(Icons.currency_exchange_outlined);
}
